# ecdump - EtherCAT Network Analyzer

## How to Use

```bash
# Basic usage (errors only)
ecdump -i eth0 -v

# Detailed output (errors + WKC)
ecdump -i eth0 -vv

# No output, just collect data for later analysis
ecdump -i eth0
```
